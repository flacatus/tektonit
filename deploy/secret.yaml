apiVersion: v1
kind: Secret
metadata:
  name: tektonit-secrets
  namespace: tektonit
type: Opaque
stringData:
  # GitHub token with repo write + PR permissions
  GITHUB_TOKEN: "ghp_REPLACE_ME"
  # Gemini API key for LLM-powered test generation
  GEMINI_API_KEY: "REPLACE_ME"
